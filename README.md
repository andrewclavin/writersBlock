# writersBlock
A reading and writing game

## Development setup

The Expo app lives in `writers-block/`.

- Recommended Node setup: install Homebrew, then `fnm`, then use the repo’s `.nvmrc` (Node LTS).

